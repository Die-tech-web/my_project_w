import { createChatView } from "./chatView.js";

export function initialiserChat(conteneurMessages, contactId) {
  // Fonction maintenue pour compatibilité
  return new Set();
}

export async function envoyerMessageChat(conteneurMessages, contactId, texte, messagesAffiches) {
  // Fonction maintenue pour compatibilité
  return null;
}

// Export de la fonction principale
export { createChatView };
